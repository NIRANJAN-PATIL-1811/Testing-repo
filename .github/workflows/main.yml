name: testing-repo-workflow

on:
  push:
    branches:
      - main

jobs:
  testing-repo-workflow-jobs:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Installation of NodeJs
        uses: actions/setup-nodejs@v4

      - name: Installing dependencies
        run: npm install

      - name: Building
        run: npm run build

      - name: Testing
        run: npm test
